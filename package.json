{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const fs=require('node:fs'); for (const file of ['package-lock.json','yarn.lock']) fs.rmSync(file,{force:true});\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "dev": "pnpm --parallel --filter @workspace/api-server --filter @workspace/grind-os run dev",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "deploy:vercel": "PNPM_HOME=/home/runner/.pnpm-global PATH=\"/home/runner/.pnpm-global:$PATH\" vercel --prod --yes"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
